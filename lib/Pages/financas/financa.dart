import 'package:flutter/material.dart';

class Financa extends StatelessWidget {
  const Financa({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'TELA FINANÇAS',
        style: TextStyle(fontSize: 24, color: Colors.green),
      ),
    );
  }
}
